import styled from "styled-components";

const DateMenuText = styled.span`
  color: white;
  font-size: 18px;
  line-height: 30px;
  letter-spacing: -0.07em;
  font-weight: 300;
`;

export default DateMenuText;
